{
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel",
     "build": "prisma generate build --output public"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "hono": "^4.4.2"
  },
  "devDependencies": {
    "vercel": "^32.4.1"
  }
}
